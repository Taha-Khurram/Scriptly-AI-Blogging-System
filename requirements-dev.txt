# Development and CI dependencies. Never installed in production -- a test
# runner and a linter in the runtime image are attack surface with no purpose.
-r requirements.txt

pytest==9.1.1
pytest-cov==6.0.0
pyflakes==3.2.0
